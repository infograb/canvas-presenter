# Security Policy

## 지원 버전

| 버전 | 보안 수정 |
|---|---|
| 최신 minor 릴리스 | 지원 |
| 이전 minor·patch 릴리스 | 최신 릴리스에서 재현되는 경우에 한해 검토 |
| 이전 major | 지원하지 않음 |

사용자는 가능한 한 최신 GitHub Release를 사용하고, 아카이브를 `SHA256SUMS`와 대조해야 합니다.

## 비공개 신고

취약점을 공개 Issue, Discussion, pull request에 올리지 마세요.

1. 저장소의 **Security** 탭에서 **Report a vulnerability**를 사용합니다: <https://github.com/infograb/canvas-presenter/security/advisories/new>
2. 비공개 신고 기능을 사용할 수 없다면 `support@infograb.net`으로 보내고 제목에 `[canvas-presenter security]`를 넣습니다.

신고에는 영향을 받는 버전, 재현 절차, 최소 manifest·자산, 기대 결과와 실제 결과, 영향 범위와 가능한 완화책을 포함해 주세요. 토큰, 비밀번호, 고객 자료와 불필요한 개인정보는 보내지 마세요.

유지보수자는 신고를 확인한 뒤 재현 가능성과 영향도를 검토합니다. 수정 전에는 공개 시점을 신고자와 조율하며, 보안 수정은 새 immutable 버전과 변경 기록으로 배포합니다. 이 공개 프로젝트에는 응답 시간 SLA가 없습니다.

## 신뢰 경계

**전제: 슬라이드는 사용자 자신의 것이거나 사용자가 검토한 것입니다.** 이 프로젝트는 그 전제 위에서 실수와 사고를 막습니다.

| 입력 | 취급 | 근거 |
|---|---|---|
| 구조 JSON | 신뢰하지 않음 | 스키마·경로·ID를 전부 검증합니다 |
| 파일 경로 | 신뢰하지 않음 | 기준 디렉터리 밖 경로와 심볼릭 링크를 거부합니다 |
| SVG·HTML 마크업 | 구조는 신뢰하지 않음 | 자체 토크나이저로 주석·문자열·속성·rawtext를 구분해 읽고, 판정도 다시 쓰기도 토큰 단위로 합니다 |
| CSS | 구조는 신뢰하지 않음 | 문자열·주석·`url()` 인자·at-rule 이름을 구분하고 이스케이프를 풀어 판정합니다 |
| 스크립트 내용 | **신뢰함** | `allowScripts: true`인 슬라이드의 JavaScript는 실행됩니다. 정적 검사만 합니다 |

마크업과 CSS는 더 이상 문맥 없는 정규식으로 훑지 않습니다. 속성 안의 `<!--`, 문자열 안의 `url()`, 네임스페이스 접두사가 붙은 `<s:script>`, 이스케이프한 `@im\70ort`, 주석 안의 `<svg width>` 같은 입력은 회귀 테스트로 고정되어 있습니다.

그래도 이것은 완전한 브라우저 파서가 아닙니다. 판정할 수 없는 입력은 통과시키지 않고 **거부**합니다. 인라인하면 의미가 달라지는 것도 거부합니다. 예를 들어 `defer`·`async`가 붙은 외부 스크립트, `media`가 붙은 스타일시트, `<head>`보다 앞에 오는 실행 가능한 내용은 오류입니다.

**검토하지 않은 제3자 HTML에 `allowScripts: true`를 지정하지 마세요.** 그 스크립트는 sandbox 안이지만 실제로 실행됩니다.

## 제공하는 방어

- manifest 기준 디렉터리 밖으로 나가는 경로와 심볼릭 링크를 거부합니다.
- 외부 SVG·HTML 자원과 지원하지 않는 active element를 거부합니다.
- 생성 HTML에 제한적인 Content Security Policy를 적용합니다.
- HTML 슬라이드는 `allow-same-origin`이 없는 sandbox iframe에서 실행합니다.
- 스크립트는 `allowScripts: true`를 명시한 슬라이드에만 허용합니다.
- 로컬 서버는 `127.0.0.1`에만 바인딩합니다.
- `--force`는 이 도구가 만든 폴더만 교체하고, 자기가 만들지 않은 파일이 있으면 멈춥니다.

브라우저 자체의 취약점, 사용자가 제한을 제거한 파생물, 외부에 다시 호스팅하며 추가한 서버 설정은 이 프로젝트의 경계 밖입니다.

## 공개 절차

보안 수정이 준비되면 유지보수자는 영향을 받는 버전, 완화책과 업그레이드 방법을 GitHub Security Advisory와 `CHANGELOG.md`에 공개합니다. 공개 전에 취약점을 악용하거나 세부 내용을 배포하지 마세요.
